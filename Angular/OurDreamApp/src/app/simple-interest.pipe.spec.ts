import { SimpleInterestPipe } from './simple-interest.pipe';

describe('SimpleInterestPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleInterestPipe();
    expect(pipe).toBeTruthy();
  });
});
